[project]
name = "talks"
version = "0.1.0"
description = "Personal repository for slide decks and presentation materials"
requires-python = ">=3.12"
dependencies = [
    "python-docx>=1.2.0",
    "python-pptx>=1.0.2",
    "pillow>=12.1.1",
]
